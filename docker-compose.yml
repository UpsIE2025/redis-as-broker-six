version: '3.8'
services:
  redis:
    image: redis:alpine
    container_name: redis-server
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    environment:
      - REDIS_PASSWORD=your_secure_password
    command: redis-server --appendonly yes
    restart: always
volumes:
  redis_data: